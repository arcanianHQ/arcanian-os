---
name: Task System Design
description: Cross-project task architecture design — connecting tasks to findings, SOPs, calendar, email, goals, and GTD tags. Ongoing design project.
type: project
---
> v1.0 — 2026-04-03

# Task System Design — Discovery & Planning

> **Status:** In progress (2026-03-23)
> **Goal:** Unified task architecture across all Arcanian projects with local .md fallback when [Task Manager] is down.

---

## 1. Projects Discovered

| Project | Location | Task File | Type |
|---------|----------|-----------|------|
| **ExampleRetail** | `_example_full/` + `[audit-framework]/clients/example-retail/` | `TASKS.md` (P0-P3 matrix, 54 tasks) | Measurement audit |
| **ExampleBrand** | `_example_full/` | `memory/tasks.md` (workstream, Asana-linked) + `takeover/MASTER-TASKLIST-4WEEKS.md` | Fractional CMO |
| **ExampleBuild** | `_ExampleBuild_full/` | **None** — has OKR/strategy docs only | Consulting client |
| **Arcanian** | `__example_full/` | **Missing** — only archived `arcanian/archive/TASKS_2026_Q1.md` | Internal ops |
| **ExampleLocal** | `_example_full/` + `[audit-framework]/clients/examplelocal/` | `TASKS.md` (P0-P3 matrix, 21 tasks) | Measurement audit + consulting ([Team Member 2]) |

---

## 2. Six Task Source Types Identified

### A. Findings (FND) — WELL CONNECTED
- **Where:** `[audit-framework]/clients/*/findings/FND-NNN_*.md`
- **Chain:** FND → REC → Task (cross-referenced in TASKS.md via `Related: FND-xxx, REC-xxx`)
- **Projects:** ExampleRetail, ExampleLocal
- **Status:** This is the gold standard. Other source types should aim for this level of traceability.

### B. SOPs / Processes — NOT CONNECTED
- **Where:** `_example_full/processes/00-21` (21 numbered process docs)
- **Where:** `[audit-framework]/methodology/MEASUREMENT_HEALTH_SOP.md`
- **Where:** `__example_full/skills/` (34 skills = methodology processes)
- **Gap:** Tasks don't reference which SOP/process they follow or were generated by.
- **Need:** `Source: SOP-01` or `Process: 05-CAMPAIGN-MANAGEMENT` in task metadata.
- **Two directions:**
  - Process **generates** recurring tasks (e.g., "every month run 04-MEASUREMENT-REPORTING")
  - Task **references** which process it follows (e.g., "onboard [Agency A] replacement → follows SOP 01")

### C. Calendar / Meetings / Events — WEAK CONNECTION
- **Where:** Inline in ExampleBrand tasks ("IT vezető meeting — [Name], 03.10 hétfő")
- **Where:** `memory/events.md` (AC London, PODIM 2026)
- **Gap:** Date is in the note text, not a structured field. No link to calendar event.
- **Need:** `Meeting: 2026-03-10 IT vezető` or similar structured reference.

### D. Emails — WEAK CONNECTION
- **Where:** Inline in ExampleBrand tasks ("[Former Team Member] email deactivation risk")
- **Gap:** Mentioned as context but not traceable to a specific email/thread.
- **Need:** At minimum, `Email: subject line / date / sender` reference.

### E. Goals (Quarterly / Yearly / Personal / Business) — NOT CONNECTED
- **Where:** `_ExampleBuild_full/DELUXE_OKR_BLUF_2026_01_20.md`
- **Where:** `__example_full/arcanian/archive/TASKS_2026_Q1.md` (phase-based goals)
- **Where:** [Task Manager] (goals feature — couldn't verify, API down)
- **Gap:** Goal documents exist but tasks don't reference which goal they serve.
- **Need:** `Goal: Q1-ship-[diagnostic-service]` or `OKR: revenue-target` in task metadata.
- **Cascade question:** Goal → Key Result → Initiative → Task? Or just Goal → Task?

### F. GTD Tags — NOT CONNECTED LOCALLY
- **Where:** [Task Manager] labels (couldn't pull — API 401)
- **Gap:** No local representation of GTD contexts (@waiting, @next, @someday, @context).
- **Need:** Tags/labels field in local task format.

---

## 3. Existing Process/SOP Inventory

### ExampleBrand — 21 Numbered Processes
```
_example_full/processes/
├── 00-PROCESS-MAP.md              # Master index
├── 01-AGENCY-COORDINATION.md
├── 02-LEAD-LIFECYCLE.md
├── 03-FINANCIAL-CONTROLS.md
├── 04-MEASUREMENT-REPORTING.md
├── 05-CAMPAIGN-MANAGEMENT.md
├── 06-EMAIL-AUTOMATION.md
├── 07-VENDOR-ACCESS.md
├── 08-CONTENT-PIPELINE.md
├── 09-SALES-MARKETING-LOOP.md
├── 10-21: Quick Wins (welcome email, deal fields, dormant automations, etc.)
├── 19-21: Databox dashboards
├── CLAY-ENRICHMENT-AC-PROCESS.md
├── MEDENCEVASAR-WEBHOOK-TO-AC.md
├── EXAMPLED2C-AC-ABANDONED-CART-BRIEF-V2.md
├── EXAMPLED2C-GHL-MIGRATION-PLAN.md
└── google-ads-api-design-doc.md
```

### [Audit Framework] — SOP + Skills
```
[audit-framework]/
├── methodology/MEASUREMENT_HEALTH_SOP.md    # Main SOP (174KB)
├── methodology/KNOWN_PATTERNS.md            # 35 patterns (PAT-001..035)
├── skills/                # /audit skill
├── skills/feed-audit/
├── skills/sgtm-diagnose/
├── skills/consent-check/
└── skills/gtm-audit/
```

### Arcanian — 34 Skills (= methodology processes)
```
__example_full/skills/
├── 7layer.md, 7layer-hu.md          # Core diagnostic
├── [customer need framework]-map, [customer need framework]-hire, [customer need framework]-outcomes, [customer need framework]-switch  # [Customer Need Framework] suite
├── [removed diagnostic suites]
├── ,      # L0 diagnostics
├── build-brand, craft-offer, validate-idea, plan-gtm, analyze-gtm
├── communication-coaching, communication-rapport
└── ... (34 total)
```

### ExampleBuild
```
_ExampleBuild_full/
├── LEAD_SIGNALS_FRAMEWORK.md
├── DRUPAL_LEAD_SIGNALS_MODULE.md
└── skills/ (contains INTEGRATED_BUSINESS_WORKFLOW.md + others)
```

### ExampleLocal
```
_example_full/
├── examplelocal-action-plan-v3.md
├── deliverables/EXAMPLELOCAL_REPAIR_ROADMAP_2026-03-19.md
└── deliverables/EXAMPLELOCAL_CONSTRAINT_MAP_2026-03-19.md
```

---

## 4. Existing Goal/Strategy Documents

| Project | Document | Type |
|---------|----------|------|
| ExampleBuild | `DELUXE_OKR_BLUF_2026_01_20.md` | OKR + BLUF |
| ExampleBuild | `DELUXE_STRATEGIC_PLAN_Q1_Q2_2026.md` | Quarterly strategy |
| Arcanian | `arcanian/archive/TASKS_2026_Q1.md` | Phase-based Q1 roadmap |
| Arcanian | `strategy/MIDMARKET_GTM_STRATEGY.md` | GTM strategy |
| ExampleBrand | `analysis/strategy.md` | Offer strategy |
| ExampleLocal | `deliverables/EXAMPLELOCAL_REPAIR_ROADMAP_2026-03-19.md` | Repair roadmap |

---

## 5. [Task Manager] — Current State

**API Status:** 401 Unauthorized (2026-03-23). Could not pull:
- Project structure (do per-client projects exist?)
- Labels/tags (what GTD labels are configured?)
- Sections (how are projects organized?)
- Filters (saved views?)

**TODO:** Re-run discovery when [Task Manager] API is restored. Key questions:
- [ ] List all [Task Manager] projects — do they mirror the 5 client projects?
- [ ] List all labels — what GTD tags exist?
- [ ] List sections per project — what structure exists?
- [ ] List saved filters — what views are configured?
- [ ] Check goals feature — any quarterly/yearly goals set?

---

## 6. Format Variations Found (5 Examples)

### Format A: Priority Matrix Table (ExampleRetail, ExampleLocal)
```markdown
---
client: "exampleretail"
last_updated: "2026-03-08"
---
### P0 — Critical
| # | Task | Priority | Status | Owner | Due | Related | Notes |
```
- **Strengths:** Structured, cross-referenced to FND/REC, clear priority
- **Weakness:** No SOP/goal/calendar/GTD links

### Format B: Workstream + Checkbox (ExampleBrand master)
```markdown
## 1. TAKEOVER — Access & Handover
### This Week
- [ ] Task description | `asana-id`
### Waiting / Blocked
- [ ] Task — blocked on X
```
- **Strengths:** Organized by workstream, Asana sync, blocked section
- **Weakness:** No priority matrix, no FND/SOP links

### Format C: Phase-based Roadmap (Arcanian archived)
```markdown
## PHASE 1: IMMEDIATE (Now — End of March)
### L5: Get Online
- [ ] **example-agency.com — activate domain**
  - Sub-detail indented
```
- **Strengths:** Strategic framing, L-layer grouping
- **Weakness:** Time-bound, gets stale, no priority/status fields

### Format D: Day-by-day (ExampleBrand 4-week)
```markdown
## HÉT 1 (feb 23-28)
### Február 24 (hétfő)
- [ ] Task per day
```
- **Strengths:** Very actionable, daily clarity
- **Weakness:** Expires fast, no metadata

### Format E: Action Plan (ExampleLocal)
```markdown
# examplelocal-action-plan-v3.md
```
- **Strengths:** Client-specific roadmap
- **Weakness:** Not a task list, no status tracking

---

## 7. Architectural Insight: Palantir Ontology Layer Analogy

> "In an ideal world, all these things would be connected to a task — like the Palantir ontology layer manages."

The Palantir Foundry Ontology is the right mental model. It doesn't store data — it **maps relationships between objects**. Applied here:

### Object Types (our ontology)
```
Task ←→ Finding (FND)
Task ←→ Recommendation (REC)
Task ←→ Process / SOP
Task ←→ Calendar Event / Meeting
Task ←→ Email Thread
Task ←→ Goal (quarterly / yearly / personal / business)
Task ←→ Person (owner, stakeholder)
Task ←→ Project (client / internal)
Task ←→ GTD Context (@next, @waiting, @someday)
```

### What this means for the local .md format:
- A task is not just "do X by Y" — it's a **node with typed edges** to other objects
- The local file is not a flat list — it's a **lightweight ontology serialized as markdown**
- Each reference field (`FND:`, `SOP:`, `Meeting:`, `Goal:`, `Email:`) is an **edge** in the graph
- This is why the ExampleRetail FND→REC→Task chain works so well — it's already ontological

### Palantir parallel:
| Palantir Concept | Our Equivalent |
|---|---|
| Object Type | Task, Finding, Process, Goal, Meeting, Email |
| Property | Priority, Status, Due, Owner |
| Link | `FND: FND-039`, `SOP: 05-CAMPAIGN`, `Goal: Q1-ship-[diagnostic-service]` |
| Action | Status change, completion, escalation |
| Ontology View | The local TASKS.md file = a view over the graph |

---

## 8. GTD Label System (CONFIRMED)

GTD-style labels are the tagging system. These map to [Task Manager] labels AND local .md tags.

### Core GTD Labels
| Label | Meaning | When to use |
|---|---|---|
| `@next` | Next physical action | Ready to do, no blockers |
| `@waiting` | Waiting for someone/something | Delegated or blocked by external |
| `@someday` | Someday/maybe | Not committed, review monthly |
| `@agenda` | Discuss at next meeting | Needs face-to-face or call |
| `@review` | Needs review/decision | Requires thinking, not doing |

### Context Labels (WHERE/HOW)
| Label | Meaning |
|---|---|
| `@computer` | Needs laptop/desk |
| `@phone` | Can do by phone |
| `@email` | Requires sending/reading email |
| `@errand` | Physical location needed |

### Energy/Time Labels (optional, GTD-adjacent)
| Label | Meaning |
|---|---|
| `@deep` | Needs focus time (>30 min) |
| `@quick` | Can do in <5 min |

### How labels interact with source types:
- A task can be `@waiting` AND linked to `Email: Bacsa NIS2 reply`
- A task can be `@next` AND linked to `SOP: 01-AGENCY-COORDINATION`
- A task can be `@someday` AND linked to `Goal: Q2-[diagnostic-service]-productize`

---

## 9. Design Requirements (Emerging)

A unified local task format needs to support:

1. **Priority** (P0-P3) — proven in ExampleRetail/ExampleLocal
2. **Status** (open, in-progress, done, blocked, wont-do)
3. **Source references (ontology edges):**
   - `FND: FND-039` (finding)
   - `REC: REC-039` (recommendation)
   - `SOP: 05-CAMPAIGN-MANAGEMENT` (process)
   - `Meeting: 2026-03-10 IT vezető` (calendar event)
   - `Email: [Former Team Member] handover 2026-03-03` (email thread)
   - `Goal: Q1-ship-[diagnostic-service]` (goal/OKR)
4. **GTD labels** — `@next`, `@waiting`, `@someday`, `@agenda`, `@review`, `@computer`, `@phone`, `@email`, `@deep`, `@quick`
5. **Owner** ([Owner], [Team Member 1], [Team Member 2], [Name], ITG, client)
6. **Due date**
7. **Project** (ExampleRetail, ExampleBrand, ExampleBuild, Arcanian, ExampleLocal)
8. **[Task Manager] sync ID** (when available)

---

## 10. Decisions Made

- [x] **GTD labels:** YES — use GTD-style labels (confirmed 2026-03-23)
- [x] **No recurring tasks:** Tasks are one-off. SOPs don't auto-generate recurring tasks. (confirmed 2026-03-23)
- [x] **Ontology model:** Palantir-style — task is a node with typed edges to other objects (confirmed 2026-03-23)
- [x] **One file per project:** Each project gets its own TASKS.md in its project root. (confirmed 2026-03-23)
- [x] **Checkbox format:** Checkboxes over tables — faster to scan, easier to edit. Metadata on indented lines. (confirmed 2026-03-23)
- [x] **Calendar/Email = text reference:** Local text reference only, no API integration. (confirmed 2026-03-23)
- [x] **SOP→Task = one-way:** Task references which SOP it follows. SOPs don't generate tasks. (confirmed 2026-03-23)
- [x] **Sync = automated + Claude-assisted:** Automated where possible, Claude fills gaps. (confirmed 2026-03-23)
- [x] **Conflict resolution = timestamp-based:** No single master. Last-write-wins using timestamps. (confirmed 2026-03-23)
  - Each task stores: `Created:` (when first written), `Updated:` (last local edit), `ext:` (external system ID), `synced:` (last sync time)
  - `Updated > synced` → push local to external system
  - `External updatedAt > synced` → pull external to local
  - Both changed since last sync → **conflict** → Claude flags for manual decision
- [x] **Timestamps format:** `Created:` and `Updated:` use `YYYY-MM-DD`. Only `synced:` uses full `YYYY-MM-DDTHH:MM` for conflict precision. (confirmed 2026-03-23)
- [x] **Timestamp line position:** Always line 4 (after GTD labels, core metadata, before ontology edges). Always present on every task. (confirmed 2026-03-23)

- [x] **Goal cascade = shallow:** Just a tag on the task (`Goal: Q1-ship-[diagnostic-service]`). No KR/Initiative hierarchy. (confirmed 2026-03-23)
- [x] **External system = per-project default + per-task override:** Each project has a default sync target ([Task Manager], Asana, Trello, etc.). Tasks inherit the project default. Individual tasks can override with a different system reference. (confirmed 2026-03-23)

## 11. External System Mapping

Each project syncs to ONE primary external system. Tasks inherit that default.

| Project | Primary System | System ID / Board |
|---------|---------------|-------------------|
| **Arcanian** | [Task Manager] | TBD (API was 401) |
| **ExampleBrand** | Asana | `EXAMPLE-ID-001` (C:ExampleBrand Transition) |
| **ExampleRetail** | [Task Manager] | TBD |
| **ExampleLocal** | [Task Manager] | TBD |
| **ExampleBuild** | TBD | TBD |

### Per-task sync fields:
```markdown
- [ ] Fix consent gating
  - sync: asana          ← which system (inherits project default if omitted)
  - ext: 000000000000  ← external system ID for this task
  - synced: 2026-03-23T14:30
  - updated: 2026-03-23T16:45
```

### Supported systems:
- `[task-manager]` — [Task Manager] (API via MCP)
- `asana` — Asana (API via MCP)
- `trello` — Trello (manual or API)
- `bitrix` — Bitrix24 (manual or API)
- `none` — local only, no sync

### Rules:
- Project-level `sync:` field in TASKS.md frontmatter = default for all tasks
- Task-level `sync:` overrides project default (rare — e.g., one task tracked in client's Bitrix while rest is in [Task Manager])
- `ext:` = the task ID in the external system (Asana task ID, [Task Manager] task ID, Trello card ID, etc.)
- Conflict resolution uses same timestamp logic regardless of external system

## 12. Task Types (9 types, confirmed 2026-03-23)

| Type | Label | Has action? | Has date? | Has owner? |
|---|---|---|---|---|
| Action | `@next` / `@computer` / `@phone` / `@email` | Yes | Usually | Yes |
| Reminder | `@reminder` | No | Yes (trigger date) | Optional |
| Waiting | `@waiting` | No (someone else) | Optional (follow-up) | Yes (who) |
| Monitor | `@monitor` | Check, not do | Yes (check date/frequency) | Optional |
| Decision | `@decision` | Think, not do | Usually (deadline) | Decision maker |
| Someday | `@someday` | Maybe later | No | No |
| Reference | `@reference` | No | No | No |
| Milestone | `@milestone` | No | Yes (fixed date) | No |

### Monitor subtypes (all use `@monitor`, distinguished by context):
- **Post-fix verification** — did the fix work? is it stable?
- **Platform health** — fragile thing, check after changes
- **Spend/performance drift** — budget or metric going off track
- **External dependency** — something outside your control that could break you
- **Compliance** — legal/consent things that must stay clean

### Monitor fields:
```markdown
- [ ] Task description
  - @monitor
  - Check: what to look at and where
  - Baseline: starting value/state (date)
  - Target: what "good" looks like
  - Risk: what could go wrong
  - Frequency: when to check (weekly / after deploys / 2x/week / etc.)
```

---

## 13. Effort & Impact Scales (confirmed 2026-03-23)

### Effort = time
`15m` | `1h` | `2h` | `4h` | `1d` | `2d` | `3d` | `1w` | `2w+`

### Impact = business outcome (5 levels)
| Impact | Meaning |
|---|---|
| `noise` | Doesn't move anything meaningful |
| `hygiene` | Prevents problems, no upside — avoids downside |
| `lever` | Multiplies something already working |
| `unlock` | Removes a blocker, opens new capability |
| `breakthrough` | Changes the game |

### Usage in task:
```markdown
- [ ] Task description
  - @next @computer
  - P1 | Owner: [Owner] | Due: 2026-03-28 | Effort: 15m | Impact: lever
```

---

## 14. File Structure (confirmed 2026-03-23)

Each project gets TWO files:
- `TASKS.md` — active tasks only (P0-P3 + Reference). Expected: 100-300 per project.
- `TASKS_DONE.md` — completed + wont-do tasks. Append-only archive.

When a task is completed:
1. Move it from `TASKS.md` to top of `TASKS_DONE.md` (newest first)
2. Add completion date: `- [x] Task description (2026-03-23)`
3. Keep all metadata (ontology edges, effort, impact) — it's historical record

`TASKS_DONE.md` frontmatter:
```yaml
---
project: "project-name"
type: archive
updated: 2026-03-23T00:00
---
```

Rules:
- Never delete from TASKS_DONE.md
- TASKS.md stays lean — only what needs attention
- Reference section stays in TASKS.md (it's active context, not done)
- Sync metadata (`ext:`, `synced:`, `updated:`) preserved in archive for audit trail

## 15. Open Design Decisions

All major decisions made. Ready to create TASKS.md files for all 5 projects.

---

## 11. [Task Manager] Discovery — BLOCKED

**API Status:** 401 Unauthorized (2026-03-23)
**TODO when restored:**
- [ ] List all [Task Manager] projects — do they mirror the 5 client projects?
- [ ] List all labels — what GTD labels already exist?
- [ ] List sections per project — what structure exists?
- [ ] List saved filters — what views are configured?
- [ ] Check goals feature — any quarterly/yearly goals set?

---

## 12. Next Steps

1. Fix [Task Manager] API access → re-run discovery (labels, projects, sections)
2. Decide on unified format (one template for all projects)
3. Create missing task files (ExampleBuild, Arcanian)
4. Backfill SOP/goal/calendar/GTD references into existing task files
5. Define sync protocol ([Task Manager] ↔ local .md)
6. Test: convert one existing project (e.g., ExampleBrand) to new format as proof of concept
