---
client: "MossTrail"
slug: "mosstrail"
scope: int-confidential
created: "2026-04-22"
---

> v1.1 — 2026-04-22 — Wired to ArcanianOS DEMO Databox account for showcase / pattern-reference use

# MossTrail — Nexus Configuration

## Purpose

Wires MossTrail to Databox per `core/methodology/NEXUS_CLIENT_CONFIG.md`. Read by `/nexus-answer` and every data-querying skill.

## Entities

| entity_id | databox_account_id | name | reporting_currency | notes |
|---|---|---|---|---|
| mosstrail-demo | 748621 | Mosstrail (ArcanianOS DEMO) | HUF | shared wiring with `clients/_demo/` — same synthetic data, accessed via explicit naming |

## Defaults

```yaml
default_for:
  general: mosstrail-demo
```

## Measurement caveats

(None — synthetic DEMO data.)

## Reporting currency

`HUF` per the DEMO data push. Note: the narrative `CLIENT_CONFIG.md` describes MossTrail as a USD-market landscaping business — that's the illustrative "story" of the scaffold. The actual Databox wiring goes to the ArcanianOS DEMO account, whose synthetic data uses HUF. So any `/nexus-answer` query against this client reads real DEMO numbers in HUF.

## Showcase wiring

This config wires the MossTrail scaffold to real DEMO data so that `/nexus-answer` produces actual 4-block output when run against this client — useful for pattern-reference, onboarding walkthroughs, and validating the resolution chain end-to-end. The hub-fallback path (`clients/_demo/`) produces identical results; this client-dir path exercises the explicit-naming route.

A typed query like *"Véget ért a Mosstrail akciós időszaka. Hogyan teljesített a kampány?"* resolves through the active-client path → reads `CAMPAIGN_REGISTRY.md` → matches the "akciós időszak" alias → queries Databox 748621 for the registered campaign window.

## Related

- `CAMPAIGN_REGISTRY.md` (this dir) — campaign window registry, mirrors `clients/_demo/CAMPAIGN_REGISTRY.md`
- `clients/_demo/.nexus-config.md` — hub-fallback twin of this config
- `core/methodology/NEXUS_CLIENT_CONFIG.md` — canonical schema
