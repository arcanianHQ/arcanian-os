---
client: "DEMO (Mosstrail)"
slug: "_demo"
scope: int-confidential
created: "2026-04-22"
---

> v1.0 — 2026-04-22

# DEMO Virtual Client — Nexus Configuration

## Purpose

This is the DEMO virtual client that `/nexus-answer` v1.7+ uses as **hub-fallback**. When a user runs the skill from a hub session (no `clients/{slug}/` CWD, no explicit client name), the skill reads this directory for entity + campaign resolution.

The DEMO is wired to **ArcanianOS DEMO Databox account 748621**, which surfaces synthetic data under the entity name *Mosstrail*. Not a real client.

## Entities

| entity_id | databox_account_id | name | reporting_currency | notes |
|---|---|---|---|---|
| mosstrail-demo | 748621 | Mosstrail (DEMO) | HUF | ArcanianOS DEMO synthetic data — single entity |

## Defaults

```yaml
default_for:
  general: mosstrail-demo
```

## Measurement caveats

(None — synthetic data, no real-world measurement issues to declare.)

## Reporting currency

`HUF` — single-entity, no cross-currency aggregation needed. Per `core/methodology/CURRENCY_NORMALIZATION.md`, the skill renders monetary metrics with `(HUF)` annotation.

## Related

- `CAMPAIGN_REGISTRY.md` (this dir) — pre-registered campaigns matching the synthetic data
- `core/methodology/NEXUS_CLIENT_CONFIG.md` — canonical schema this file follows
- `core/skills/nexus-answer.md` — consumer skill (v1.7+ uses this DEMO fallback)
